- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/kitty:
      create: true
      force: true
      relink: true
      path: kitty

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [sh install.sh, Installing packages]
